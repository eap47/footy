

for (var i=0;i<fixtures.length;i++) {
	if ( (fixtures[i].HomeTeam === teamA || fixtures[i].AwayTeam === teamA)  && (fixtures[i].HomeTeam === teamB || fixtures[i].AwayTeam === teamB) ) {
		var selectedFixtures = [];
		selectedFixtures.push(fixtures[i]);

	}

}

